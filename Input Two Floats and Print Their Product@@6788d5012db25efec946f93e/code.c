#include <stdio.h>

int main() {
    // Intialising the float Values
    float a,b;
    // enter the values
    scanf("%f %f",&a,&b);
    // print the product
    printf("Product: %.4f",a*b);
    return 0;
}