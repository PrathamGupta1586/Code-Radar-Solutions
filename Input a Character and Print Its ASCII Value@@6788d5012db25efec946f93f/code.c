#include <stdio.h>

int main() {
    // Intialising a character
    char a;

    // Taking a input Character
    scanf("%c",a);

    // Printing an ASCII Value
    printf("ASCII Value: %d",(int)a);

    return 0; // Returns the value
}